import { NextResponse } from "next/server"; import { runFeedSync } from "@/lib/importFeed"; export const maxDuration = 300; function checkAuth(req: Request): boolean { const authHeader = req.headers.get("authorization"); const expected = "Basic " + Buffer.from(`admin:${process.env.ADMIN_PASSWORD}`).toString("base64"); return authHeader === expected; } export async function GET(req: Request) { if (!checkAuth(req)) { return NextResponse.json({ error: "Jogosulatlan" }, { status: 401 }); } try { const { searchParams } = new URL(req.url); const limitParam = searchParams.get("limit"); const offsetParam = searchParams.get("offset"); const limit = limitParam ? parseInt(limitParam, 10) : 250; const offset = offsetParam ? parseInt(offsetParam, 10) : 0; const summary = await runFeedSync(limit, offset); return NextResponse.json({ ok: true, summary }); } catch (err: any) { return NextResponse.json({ ok: false, error: err?.message ?? String(err) }, { status: 500 }); } }
