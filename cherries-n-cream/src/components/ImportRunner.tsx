"use client"; import { useState, useRef } from "react"; interface RunLog { time: string; message: string; } export default function ImportRunner() { const [running, setRunning] = useState(false); const [dbCount, setDbCount] = useState<number | null>(null); const [needsReview, setNeedsReview] = useState<number | null>(null); const [feedTotal, setFeedTotal] = useState<number | null>(null); const [logs, setLogs] = useState<RunLog[]>([]); const stopRef = useRef(false); function log(message: string) { setLogs((prev) => [{ time: new Date().toLocaleTimeString("hu-HU"), message }, ...prev].slice(0, 30)); } async function checkProgress() { const res = await fetch("/api/admin/progress"); if (!res.ok) return; const data = await res.json(); setDbCount(data.count); setNeedsReview(data.needsReview); } async function runLoop() { setRunning(true); stopRef.current = false; log("Import elindítva…"); while (!stopRef.current) { try { const res = await fetch("/api/admin/sync"); const data = await res.json(); if (!data.ok) { log(`Hiba: ${data.error ?? "ismeretlen hiba"}`); break; } const s = data.summary; setFeedTotal(s.total); log(`Köteg kész — feldolgozva: ${s.created + s.updated}/${s.total}, hibás: ${s.errors?.length ?? 0}, felülvizsgálandó: ${s.needsReview}`); await checkProgress(); if (s.created === 0 && s.updated === 0) { log("Nincs több feldolgozandó termék — az import kész!"); break; } } catch (err: any) { log(`Hálózati hiba: ${err?.message ?? err} — újrapróbálkozás...`); } } setRunning(false); } function stop() { stopRef.current = true; log("Leállítás kérve — a folyamatban lévő köteg még befejeződik."); } return ( <div className="space-y-6"> <div className="flex items-center gap-4"> <button onClick={runLoop} disabled={running} className="px-6 py-3 bg-cherry hover:bg-cherryLight disabled:opacity-50 rounded-soft font-medium"> {running ? "Fut…" : "Import indítása"} </button> {running && ( <button onClick={stop} className="px-6 py-3 border border-cream/30 rounded-soft"> Leállítás </button> )} <button onClick={checkProgress} className="px-4 py-3 text-sm text-cream/60 hover:text-cream underline"> Állapot frissítése </button> </div> <div className="grid grid-cols-3 gap-4 text-center"> <div className="border border-cream/10 rounded-soft p-4"> <p className="text-2xl font-medium">{dbCount ?? "–"}</p> <p className="text-xs text-cream/50 mt-1">Termék az adatbázisban</p> </div> <div className="border border-cream/10 rounded-soft p-4"> <p className="text-2xl font-medium">{feedTotal ?? "~14 500"}</p> <p className="text-xs text-cream/50 mt-1">Termék a feedben</p> </div> <div className="border border-cream/10 rounded-soft p-4"> <p className="text-2xl font-medium">{needsReview ?? "–"}</p> <p className="text-xs text-cream/50 mt-1">Felülvizsgálandó</p> </div> </div> <div> <p className="text-sm text-cream/60 mb-2">Napló</p> <div className="border border-cream/10 rounded-soft p-4 h-64 overflow-y-auto text-xs space-y-1 font-mono"> {logs.length === 0 && <p className="text-cream/30">Még nincs esemény.</p>} {logs.map((l, i) => ( <p key={i} className="text-cream/70"> <span className="text-cream/40">{l.time}</span> — {l.message} </p> ))} </div> </div> </div> ); }
